<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ArenaFight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Fond global -->
        <item name="android:colorBackground">@color/gris</item>
        <item name="android:windowBackground">@color/gris</item>
    </style>

    <style name="Theme.ArenaFight" parent="Base.Theme.ArenaFight"/>

</resources>